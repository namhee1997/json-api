<!-- 

 git init

git add .

git commit -m "a"

git config --global user.email "namkaka2612@gmail.com"

git config --global user.name "Nam-Nee"

git status

git commit -m "a"

git remote add origin git@github.com:Nam-Nee/json-server.git


git push origin main

git push -u origin main


 -->